<?xml version="1.0" encoding="UTF-8"?>
<sitemesh>
  <!-- Map default decorator. This shall be applied to all paths if no other paths match. -->
  <!-- <mapping decorator="/decorator/default-decorator.jsp"/> -->

  <!-- Map decorators to path patterns. -->
  <!-- <mapping path="/layout/*" decorator="/decorator/default-decorator.jsp"/> -->
  <!-- <mapping path="/*.special.jsp" decorator="/special-decorator.html"/> -->

  <!-- Alternative convention. This is more verbose but allows multiple decorators
       to be applied to a single path. -->
  <mapping>
    <path>/*</path>
    <decorator>/decorator/mainMenu-decorator.jsp</decorator>
<!--     <decorator>/decorator/logo-decorator.jsp</decorator> -->
  </mapping>

  <!-- Exclude path from decoration. -->
  <!-- <mapping path="/javadoc/*" exclue="true"/> -->
  <!-- <mapping path="/brochures/*" exclue="true"/> -->

</sitemesh>